<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DEVPROG_Project.Views.CharacterListPage">
    <ContentPage.Content>
        <ListView x:Name="lvwCharacterList" RowHeight="50" SeparatorColor="Transparent" ItemSelected="lvwCharacterList_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame BackgroundColor="WhiteSmoke" Margin="8" Padding="8,0">
                            <Grid>
                                <Label Text="{Binding FullName}" TextColor="Black" VerticalOptions="Center" />
                                <Label Text="..." TextColor="Gray" VerticalOptions="Center" HorizontalOptions="End" />
                            </Grid>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>